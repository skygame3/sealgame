# Review — TASK-008 Internationalization Setup

- **Date**: 2026-07-30
- **Reviewer**: Codex (self-review)
- **Decision**: Approved

## Scope reviewed

- i18next initialization with Korean and English resources.
- Device-language resolution (`ko` or English fallback), explicit language switching, and
  English fallback for missing translations.
- Expo localization plugin registration and a small re-export surface for application code.
- Key-based strings in the initial screen.

## Verification

- Korean and English locale keys are checked for exact parity.
- Tests confirm language selection and translated output.
- `npm run format`, `npm run lint`, `npm run typecheck`, `npm test`, and
  `npx expo install --check` all passed.

## Checklist

- Engine code remains independent of i18n.
- No UI string added by this task is hard-coded in the rendered screen.
- New M0 dependencies are recorded in `dependencies.md`.
