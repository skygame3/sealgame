# Review — TASK-006 MVP H Board

- **Date**: 2026-07-30
- **Reviewer**: Codex (self-review)
- **Decision**: Approved

## Scope reviewed

- `boards/h.json` defines the MVP H board with 13 nodes, 14 edges, two locked expansion
  nodes, and central key node `c`.
- The board is included in the automated validation suite.
- The design-spec edge example was corrected to follow the documented normalized ID order;
  this does not alter the graph's connections or game rules.

## Verification

- `validateBoard(hBoard)` returned no issues.
- The H board JSON parsed successfully.
- `npm run format`, `npm run lint`, `npm run typecheck`, and `npm test` passed.

## Checklist

- All coordinates remain normalized in the 0–1 range.
- Both locked nodes are declared in `lockedNodes` and the one core node in `keyNodes`.
- The graph is connected and exposes capture-capable paths.
- No core rule or balance parameter was changed.
